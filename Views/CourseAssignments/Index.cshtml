@model IEnumerable<MVCUniversityRegistration.Models.CourseAssignment>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Instructor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Instructor.FirstMidName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Course.CourseID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CourseAssignmentID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CourseAssignmentID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CourseAssignmentID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
